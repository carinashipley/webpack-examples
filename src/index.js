import React from 'react';
import ReactDOM from 'react-dom';

const elementId = "react-app";

ReactDOM.render(
  <div>
    This is the React App, which isn't really doing much of anything. This is just to demonstate Webpack.
  </div>,
  document.getElementById(elementId));
